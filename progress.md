# Progress

## 2026-07-17 — 立项与证据流水线

- 初始化 `main` 仓库，定义产品名“标擎 BidPilot”和团队名“聚标成擎”。
- 核验超聚变命题、报名表、赛制、截止时间和交付要求。
- 完成中文查询编译器、Pydantic 领域模型、SQLite 基础结构。
- 完成中国招标投标网、中国政府采购网、千里马授权源骨架。
- 完成严格筛选、证据摘要、转载去重、生命周期聚类、机会评分和 DOCX 报告。
- 完成 FastAPI、CLI 和第一版 Web 闭环。

## 2026-07-18 — 真实工程化里程碑

### 来源

- 来源从 3 个扩展到 5 个，新增全国公共资源交易平台、商务部中国国际招标网。
- 全国平台只使用首页最新公告流，始终标记 `partial`，不冒充全量历史检索。
- 商务部适配器使用真实表单接口，已完成在线查询、详情解析和证据保留。
- 千里马未授权时返回 `auth_required`，等待用户主动登录免费会员。

### 长期任务

- 移除 APScheduler 进程内闹钟，改为 SQLite 持久 worker。
- 支持 `python -m bidpilot worker` 和 Web 内嵌 worker。
- 持久化 `next_run_at`、运行、投递尝试、失败次数、worker 心跳、租约和增量账本。
- 实现重启补跑、失败退避、多 worker 原子领取、长任务续租和手动/自动执行互斥。
- 故障注入验证：投递失败不写账本；首次重试约 60 秒；租约未过期不能重复领取；过期后可接管。

### 投递与管理

- 本地报告中心明确标记“不主动推送”。
- 飞书 Webhook 支持运行卡片和下载链接；飞书应用支持 Word 文件与无新增文本回执。
- 新增跨平台标准 SMTP：SSL/STARTTLS、Word 附件、多收件人、无新增回执。
- 未配置渠道在 UI 禁用，创建订阅时拒绝虚假推送承诺。
- 订阅中心支持编辑自然语言规则、通知策略、渠道、暂停、恢复、立即执行、日志和二次确认删除。
- 新增来源中心：官方/行业属性、接入模式、授权状态、最近检查、抓取/保留和诊断。
- 修复 1280px 下来源浮层覆盖查询面板；编辑期间自动刷新不再清空输入。

### 跨平台与部署

- 删除 `setup.ps1`，新增纯 Python `bootstrap.py`。
- 新增非 root Dockerfile、Compose Web + 独立 worker、持久卷、健康检查和自动重启。
- Compose 补齐 LLM、会员会话、飞书和 SMTP 环境变量。
- 新增 Windows/macOS/Linux、Python 3.11/3.13 GitHub Actions 矩阵。
- 本机没有 Docker，不能把镜像构建标记为已验证。

### 真实验收证据

- 订阅 `f8b24edfbe64481dbcc7d4fdee85d94f` 在真实进程重启前后保持同一 ID、`next_run_at`、运行历史和投递历史。
- 首轮真实查询“最近1个月江苏芯板招标信息，请每天9:00发送给我”新增 5 条并生成 DOCX。
- 浏览器点击第二轮“立即执行”新增 0 条，仍记录本地运行回执；运行数 2、投递尝试 2、账本仍为 5。
- 浏览器完成临时订阅的规则编辑、计划重算、暂停、恢复和二次确认删除。
- 来源中心真实显示 5 个来源，其中 4 个当前可运行、3 个官方平台；千里马待授权。
- 最终门禁：36 项 Pytest、Ruff lint/format、JavaScript 语法、JSON/YAML、依赖完整性、敏感令牌扫描和 wheel 构建全部通过。
- wheel 构建门禁曾发现静态资源重复收录，已移除 Hatch 重复配置；最终生成 `bidpilot-0.2.0-py3-none-any.whl`。

## 2026-07-18 — 机会工作台里程碑

- 新增项目级 `opportunities` 持久表与 REST API，只允许从 `tender_items` 的真实标讯创建机会。
- 同一 `project_key` 重复加入保持同一机会 ID，不重复生成线索，也不改动已稳定的人工状态。
- Web 新增机会工作台：阶段看板、搜索、筛选、负责人、下一步、标签、备注、已读状态和归档管理。
- 生命周期时间线保留同项目全部已抓取事件与原文链接；同日事件按采购意向、招标、更正、中标、合同阶段择新。
- 后续真实生命周期事件自动刷新机会快照并重新标为未读，同时保留阶段、负责人、待办、标签和备注。
- 真实浏览器使用“南通深南电路有限公司项目-芯板立体库”完成加入、编辑、搜索、筛选、已读和生命周期闭环；时间线包含招标与更正两个真实来源事件。
- 两次真实进程重启后，同一机会 ID、跟进中阶段、负责人、下一步、标签和备注均保持；最新更正公告正确成为卡片快照。
- 筛选无结果提供一键清空操作，空工作台明确引导从真实检索加入，不使用演示假数据。
- 最终门禁：38 项 Pytest、Ruff lint/format、JavaScript 语法、JSON/YAML、依赖完整性、OpenAPI、令牌扫描、`git diff --check` 和 wheel 内容检查全部通过。
- 生成 `bidpilot-0.3.0-py3-none-any.whl`（81,647 bytes，SHA-256 `b5c9b1a8fc7845cc928139410c3e44630937a00d35a784d59a862b8d34745b54`）；静态 Web 资源与 API 代码已在包内复验。

## 当前约束

- Git 远端已配置为 `https://github.com/gitchw/bidpilot.git`；v0.7.0 发布必须同步 `main`、全部本地分支和全部标签，并以远端引用复核为准。
- 本机没有 Docker；需要在有 Docker 的环境实际执行 `docker compose up -d --build`。
- 飞书真实投递需要用户提供 Webhook 或应用凭据；SMTP 真实投递需要应用专用密码。
- 千里马真实链路需要用户主动完成免费会员登录，Cookie 不得提交仓库。
- 比赛材料和最终表单提交按用户要求暂缓；任何对外提交仍需动作时确认。

## 2026-07-18 — v0.7 产品重审与广东政府采购深搜

- 重新审视完整用户旅程，将下一阶段从“增加搜索入口”收敛为企业画像、证据约束适配判断、本轮证据追问、反馈学习和买方雷达五个可验证闭环，并把 F39–F44 追加到 append-only 功能清单。
- 新增第 10 个真实来源“广东省政府采购网”：使用官方 `selectInfoForIndex` 全文检索和 `getInfoById` 详情接口，覆盖广东省及 21 个地级市地域路由，非广东查询明确 `skipped`。
- 列表解析移除站内关键词高亮标签但不破坏原题名；详情保留采购人、项目编号、公告阶段、正文和官方附件，并对采购计划模板只有列表字段的情况诚实标记 `partial`。
- 发现官网 SPA 的 history 详情路由内部点击可用、直接打开却返回 HTTP 403；证据链接改用同内容的官方公开详情 API，在线抽验 5/5 均为 HTTP 200，不把不可直达的漂亮链接伪装成可用证据。
- 在线烟测“最近1个月广东服务器采购信息”扫描并返回 20 条广东官方候选，其中 14 条读取完整公开正文与附件、6 条计划模板保留列表证据；解析夹具、地域零网络跳过、相关回归与 Ruff 均通过。

## 2026-07-18 — 可配置智能中枢与解析器 v2

- 修复截图原句“最近1个月深圳充电桩招标信息”：主题为“充电桩”、地域为“深圳”、来源地域代码为 `440000`，并在真实浏览器完成点击验收。
- 意图解析器升级为 `rules-v2`：支持常见城市、自治区全称、中文数字时间窗、半年/季度/本周/上周/今天/昨天、中文时钟、每日/每周/每月/月末/一次性计划、排除词和显式公告类型。
- 普通“招标信息/采购信息”不再错误限制为招标公告，完整保留更正与中标生命周期；明确“只看招标公告”等表达仍严格过滤。
- 字段级意图矩阵扩展到 30 条以上，覆盖截图原句、否定公告类型、月底计划与多种通道；相关解析、生命周期和调度回归通过。
- 新增网页配置中心：OpenAI-compatible 模型、飞书群机器人/应用、SMTP、钉钉、企业微信、通用 Webhook 均可网页管理，保存立即生效并在进程重启后恢复。
- 配置写入只接受白名单字段并要求短期编辑令牌；敏感值使用本机 Fernet 密钥加密后存入 SQLite，读取 API、状态、日志与错误只显示配置状态。
- 使用用户提供的本机 OpenAI-compatible 服务完成真实模型连通测试，模型回复正确，密钥未进入仓库、文档、日志或 API 响应。
- 新增钉钉 HMAC 加签、企业微信 Markdown 和通用 JSON Webhook；HTTP/SMTP 错误统一脱敏，避免含 token 的 URL 进入运行日志。
- `/docs` 的 29 个 API 操作全部增加中文用途、参数、返回、副作用、错误码和示例，并新增 API、配置、渠道、用户四份独立文档。
- 最终门禁：85 项 Pytest、Ruff lint/format、JavaScript 语法、JSON/YAML、依赖完整性、OpenAPI 文档契约、敏感模式扫描、`git diff --check` 和 wheel 构建全部通过。
- 生成 `bidpilot-0.4.0-py3-none-any.whl`（105,842 bytes，SHA-256 `89e262186409f4eede30a98d651124c43ed9a7418c690b7df502fba47424e7fb`），复验包含配置中枢与静态 Web 资源。

## 2026-07-18 — v0.5.0 混合意图、可解释检索与零基础交付

- 混合意图引擎升级为 `hybrid-v1`：`rules-v2` 始终先生成可执行基线，仅低置信、缺失或冲突字段才允许 LLM 提议；严格 JSON、地域、日期、计划、枚举、字段锁定和逐字段决策共同约束合并。
- 浏览器复测“最近1个月深圳充电桩招标信息”，稳定得到主题“充电桩”、地域“深圳”、近 1 个月和立即执行；规则高置信时明确显示“无需调用 LLM”。
- 零结果不再只显示“0 条”：新增扫描 → 候选 → 保留漏斗、逐项排除原因、来源覆盖缺口和最多三条不会自动执行的安全放宽建议；即时任务为 0 条时仍生成 Word 诊断报告。
- 网页配置中心覆盖 OpenAI-compatible 模型与飞书、SMTP、钉钉、企业微信、通用 Webhook；敏感值加密保存且 API 只返回配置状态，网页可保存、清除和真实测试，无需手改配置文件。
- `serve/status/stop/restart` 形成完整生命周期控制；真实优雅停服和新进程启动后，订阅 ID、启用状态、`next_run_at`、投递策略及脱敏模型配置逐字段一致，内嵌 worker 自动恢复在线。
- OpenAPI 共 31 个操作，全部具备中文用途、参数、返回值、副作用、常见错误和示例；Swagger 与手册统一为最终解析器 `hybrid-v1`、规则基线 `rules-v2`。
- 新增可复现构建器 `tools/build_beginner_manual.py`，生成 43 页《标擎 BidPilot 零基础操作说明书》：真实目录页码、4 张实机截图、跨平台安装、启动/停止/重启、订阅、模型与通道、备份恢复、故障树、术语表和命令速查完整覆盖。
- Word 说明书通过业务规则校验、Word 原生 PDF 导出、43 页逐页视觉复核、标题/章节/字段/图片/表格/可访问性审计；无裁切、重叠、孤立尾页或大片异常空白。简化 XSD 对关系命名空间的误报已由业务校验与 Word 原生打开双路径替代验证。
- 最终工程门禁：97 项 Pytest、Ruff lint/format、JavaScript 语法、JSON/YAML、依赖完整性、OpenAPI 合约、敏感凭据扫描、`compileall`、`git diff --check` 和 wheel 内容检查通过。
- 生成 `bidpilot-0.5.0-py3-none-any.whl`（131,674 bytes，SHA-256 `d7f16174830101b38157cbf545534a263ca94e832142fb58ffaa23f4035e9db1`），确认包含混合意图、控制面与静态 Web 资源且不包含运行数据或密钥。

## 下一步产品优先级

1. 采购单位/竞争对手监控与高级组合规则。
2. 命中原因解释、可配置机会评分和机会待办日历。
3. 来源健康历史和适配器回归监测。
4. 核心产品稳定后再生成报名资料、详设、操作手册、PPT/PDF 和演示视频脚本。

## 2026-07-18 — v0.6.0 AI 深度检索重构启动

- 新增 v0.6 硬性验收契约：检索计划、多轮补搜、语义边界复核、至少 8 个真实来源、统一网页授权、固定召回评测与完整安全门禁。
- 将 F27–F32 追加到功能清单，保持 append-only；在真实实现和验收完成前全部保持 `pending`。
- 建立无业务写入的 `tools/evaluate_retrieval.py` 固定评测器；强制关闭模型，不写 SQLite、不生成报告、不触发投递，只记录真实扫描、候选、保留、证据 URL、来源诊断与排除原因。
- 2026-07-18 15:03 基线实测四类查询共扫描 1,258 条、进入统一过滤 44 条、最终保留 2 条、对应 4 个证据 URL；深圳充电桩与江苏数据中心均为 0 条，北京医疗设备和全国服务器各保留 1 条。
- 基线证实当前瓶颈：CCGP 两页固定公告流共贡献大部分扫描但缺少站内关键词召回；CEC 搜索结果多数超出时间；GGZY 仅首页流；Mofcom 稀疏；千里马未授权。后续新版必须使用同一评测器对照，不以主观观感宣称提升。
- 安全边界保持不变：只使用公开入口、合理缓存/限速和用户主动授权会话；不破解验证码、CA、付费墙、账号权限、访问控制或频率限制。

### 固定召回质量门（逻辑单元 3）

- 固定四类中文查询的新版实测共扫描 1,441 条、进入统一过滤 224 条、最终保留 114 条，对应 126 个唯一证据 URL；四类查询相对 v0.5 基线全部提升。
- 在线逐条验证 126 个证据入口，最终 126 个可直接访问，失效、网络异常和未验证均为 0；质量门 `benchmark.passed=true`。
- 发现深圳官方列表 API 仍返回 `/static/...` 和 `:8081` 退役入口，适配器改用相同 `contentId` 的现行 `/jygg/details.html` 官方详情页，并以真实 HTTP 200 和回归测试双重验证。
- 评测结果保存每条标题、日期、地域、来源、评分和 URL，并区分可访问、原站限制、失效、网络异常，避免把 403/429 误报为链接失效。

### AI 检索轨迹与网页控制（逻辑单元 4）

- 结果页以真实后端轨迹展示受控检索词、AI 规划状态、两轮来源调用、扫描/候选/去重数量、缺口原因、硬过滤、语义复核和最终保留，不使用前端推算替代后端数据。
- 配置中心补齐 AI 检索规划开关、最多轮次、每来源查询预算、语义复核开关和接受阈值；真实浏览器修改、保存、页面重载后五项均持久化，再恢复推荐值。
- 正向实测“最近1个月深圳充电桩招标信息”：AI 接受 6 个扩展词，9 次首轮来源调用，扫描 335、候选 30、保留 18、重复合并 2，页面与 API 一致。
- 零结果实测“最近1周深圳量子芯片招标信息”：首轮 9 次调用后触发第二轮 12 次调用，扫描 334、候选 30、保留 0；页面同时解释过滤原因、来源覆盖边界和安全放宽建议。
- 检索词来源、边界复核状态和模型发送边界改为明确中文；390×844 移动视口无横向溢出，浏览器控制台无产品 JavaScript 错误。

### 证据约束 AI 情报副驾驶（逻辑单元 5）

- 每轮可信结果按机会分建立最多 25 条的本地 `E01` 证据目录；模型只接收网页配置上限内的标题、采购人、日期、阶段、评分和 500 字片段，不接收 URL、记录 ID、订阅历史、机会备注或密钥。
- 严格 JSON 只允许返回采购需求、证据编号、优先理由、风险和建议动作；标题、采购人、日期、阶段、评分和来源 URL 全部由本地证据目录回填。未知证据、额外字段或证据中不存在的数字会拒绝整份输出。
- 增加一次有界自动修复：首次模型输出违反 JSON/证据契约时只反馈脱敏失败类型并重试一次；连续失败立即回退确定性分析，不中断检索、报告或投递。
- SQLite 按查询口径、记录版本、模型和提示词版本缓存仅通过验证的 `llm_grounded` 简报；真实复用返回 `status=cached`、`cache_hit=true`，失败回退不污染缓存。
- 真实模型实测“最近1个月深圳超充站招标信息”保留 9 条记录，生成采购需求、3 条优先机会、风险与分级行动，并在网页和 Word 报告中显示 `E` 编号和可点击原文；390×844 无横向溢出，控制台无产品错误。
- 首次真实输出包含证据未出现的数字时被正确拒绝；强化禁止计算/换算/估算的提示后，同一批 9 条证据真实得到 `mode=llm_grounded`、`status=applied`，证明安全回退和真实 AI 成功两条路径均可用。

### 来源健康历史与趋势（逻辑单元 6）

- 新增只读 `/api/v1/sources/health?window=20`：按来源聚合 5–100 个真实运行样本，返回状态计数、完全健康率、完成率、平均/P95 延迟、候选保留率、延迟/产出趋势和逐次脱敏诊断。
- 健康判定明确区分 `healthy`、`degraded`、`unhealthy`、`auth_required` 和 `no_data`；按地域跳过计为已完成而不是来源故障，等待用户登录也不伪装成站点失败。
- 来源中心并行读取即时状态和 SQLite 历史，每张来源卡展示状态序列、统计指标、趋势解释和可展开的最近运行明细；不为查看健康而访问外站或刷新 Cookie。
- 真实浏览器读到 9 个来源共 116 个样本：5 个健康、3 个降级、1 个待授权；深圳来源展开显示 4 次时间、状态、扫描/候选/保留和耗时，千里马明确显示等待授权。
- 390×844 移动视口下健康指标和历史明细切换为双列且无横向溢出；浏览器控制台无产品 JavaScript 错误。

### AI 检索内核（逻辑单元 1）

- 新增 `retrieval-v1` 检索计划：确定性同义词与本地行业词典始终可用；模型仅用严格 JSON 提出词条、查询变体和来源优先级，通用词、日期、URL、地域冒充主题、低置信与控制语句均在本地拒绝。
- 使用已保存的 OpenAI-compatible 服务完成真实规划调用；“北京医疗设备”在本地 6 个概念基础上增加医用呼吸机、医学影像设备、生命监护仪等 6 个经校验词条，模型响应和密钥均未持久化到运行轨迹。
- 流水线升级为最多两轮：所有来源先跑核心主题；匹配候选少于 5 条时，只对声明支持关键词变体且首轮可用的来源执行一次补搜，固定列表/首页流不重复扫描，来源 URL 在摘要前去重。
- 时间、地域、公告类型和排除词拆成不可覆盖的硬过滤；仅硬过滤通过但无字面命中的候选进入批量语义复核，接受阈值默认 0.82，模型缺失、超时、格式错误或漏回候选时均保守拒绝。
- 改进同义词相关度：受控同义词命中标题/正文获得明确语义权重，避免“有同义词命中却因原主题不在标题而低于 45 分”的二次误杀。
- `RunResult` 与 SQLite 新增向后兼容 `retrieval_json`：持久保存计划、每轮查询/调用/扫描/候选/缺口、语义决策与总览，不保存模型原文、密钥或 Cookie。
- 5 个既有适配器补充稳定来源 ID、公开/会员模式、查询方式、分页/地域/日期/详情能力与真实覆盖说明，为补搜调度和图形化来源中心提供统一元数据。
- 新增 5 项检索专项测试，覆盖本地扩词、固定源不重复、严格 JSON 校验、模型格式回退、语义接受和地域硬过滤不可越权；完整 102 项 Pytest 与 Ruff 通过。
- F27–F29 已在功能清单中按实际验证证据标记通过：真实模型规划、无模型回退、两轮补搜、URL 去重、地域硬过滤与语义复核均有自动化测试或在线轨迹支撑。

### 官方来源扩展（逻辑单元 2）

- 来源从 5 个扩展到 8 个，新增中央政府采购网、军队采购网和中国招标投标公共服务平台；三者均具备独立适配器、稳定来源 ID、能力元数据、解析夹具和失败隔离。
- 真实在线烟测“最近1个月全国服务器采购信息”：公共服务平台返回 20 条公开列表摘要并诚实标记 `partial`；军队采购网返回 20 条公开正文并标记 `ok`；中央政府采购网返回 20 条电子卖场公告但详情受网关限制，因此标记 `partial`。
- 不穿透公共服务平台的 WAF/VAPTCHA，不绕过中央政府采购电子卖场详情网关，不代替用户完成军队采购工作台登录或 CA；受限范围全部写入来源能力说明和运行诊断。
- 新增 `tools/smoke_sources.py` 与 `outputs/evaluations/source-smoke-v0.6.json` 作为可复现在线证据；完整 102 项 Pytest、Ruff lint/format 与 `git diff --check` 通过。

### 深圳地方官方深搜（逻辑单元 3）

- 新增第 9 个真实来源“深圳公共资源交易中心”，通过其公开 `/cms/api/v1/trade/content/page` 与详情接口覆盖政府采购、建设工程和阳光采购；保留官方正文、采购人、项目编号、公告类型和附件。
- 地域路由只在全国、广东或深圳查询中启用；北京、江苏等无关地域明确返回 `skipped`，不浪费查询预算，也不把“未调用”伪装成抓取失败。
- 真实在线烟测“最近3个月深圳充电设施采购信息”在深圳官方源返回 20 条候选；全国服务器烟测亦返回 20 条深圳官方候选。用户登录、投标工作台和 CA 不属于公开检索，能力边界已写入来源元数据。
- `HttpFetcher` 新增受限速、重试和统一诊断约束的 JSON POST 能力；新增深圳列表/详情夹具和解析回归。完整 106 项 Pytest、Ruff lint/format 与差异检查通过。

### 统一来源授权与图形化来源中心（逻辑单元 4）

- 新增 `SourceAuthManager` 与 `source_authorizations` 表：千里马、中国招标投标网均可从网页启动可见 Chromium，由用户本人登录后点击完成；只采集允许域名 Cookie，使用独立本机 Fernet 密钥加密存入 SQLite。
- 授权生命周期完整覆盖开始、状态、完成、真实测试和清除；所有写操作同时要求回环客户端与 10 分钟编辑令牌。API、来源状态、错误与测试均不返回 Cookie 名称、值、账号、密码、验证码或 CA 信息。
- 旧版千里马明文 Cookie 文件首次启动自动加密迁移并删除明文；CLI `auth` 复用同一管理器并扩展到 `qianlima/cecbid`，不再拥有单独的明文保存路径。
- 来源中心真实展示 9 个来源的查询方式、地域/日期/分页/正文能力、扫描/候选/保留、耗时、覆盖限制、授权真实作用范围和操作按钮；军队采购、公共服务平台等授权不能扩大当前检索的来源只提供原站入口，不伪装成“已增强”。
- `python bootstrap.py --auth` 现在一次完成 Playwright 与 Chromium 安装；真实可见浏览器启动烟测通过并自动关闭，未代替用户登录。109 项 Pytest、Ruff 与 JavaScript 语法检查通过，浏览器 DOM 与全页视觉验收通过。

## 2026-07-18 — 证据约束企业适配引擎（逻辑单元 7）

- 新增逐条企业适配判断：每条本轮可信标讯都得到“建议参与 / 持续观察 / 暂不投入”、本地适配分、风险、下一步和可点击原文；模型窗口外的记录不会丢失，而是继续使用同一画像做确定性判断。
- 模型职责严格限制为识别画像语义关联并逐字摘录当前 E 编号的公告证据；模型不能提交分数、推荐、标题、采购人、日期、地域、阶段或 URL，所有身份与决策字段均由本地计算或回填。
- 严格 JSON 整批校验覆盖额外字段、遗漏/重复/未知 E 编号、未知画像词、非原文摘录和提示注入式自由文本；只允许一次脱敏修复，连续失败、超时或未配置模型时安全回退，不中断检索。
- 本地硬边界读取标题、摘要、正文片段、完整证据和附件名；排除词、结果阶段和城市级地域不匹配不能被模型或反馈突破。广东目标可包含深圳，深圳目标不会误把广州当作命中。
- 相关/无关/观察/已联系反馈的相似调整严格限制在 -12～+12；当前公告的直接反馈优先于相似历史，不会出现“已明确无关却被相似正反馈推高”的冲突解释。
- 适配缓存键覆盖完整证据快照、画像、反馈、模型端点/名称、开关和窗口；证据、画像、反馈或模型变化都会失效。刷新历史运行后同时更新 SQLite 与内存结果，进程重启仍可从固定 `run_items` 恢复。
- 使用用户已保存的 OpenAI-compatible 服务完成真实测试：严格输出首次未通过后自动修复一次，最终 `llm_grounded/applied`，1/1 结果引用 3 条经逐字验证的证据；密钥和模型原始响应未写入仓库、数据库、日志或 API。
- 相关 36 项 Pytest、Ruff lint/format 与 `git diff --check` 通过；独立审计提出的模型间接改分、完整证据排除词遗漏、同省城市误判、E 编号伪摘录和直接反馈反转均已转成回归测试并修复。

## 2026-07-18 — 本轮固定证据追问（逻辑单元 8）

- 新增 `POST /api/v1/runs/{run_id}/ask`：只读取指定运行按 `position` 持久化的 `run_items` 快照，不回查全局公告、不重新访问来源，后来抓取的新记录不会串入旧运行。
- 模型不能自由撰写事实答案，只能返回严格 JSON 的 `answerable` 与最多 8 条 `E 编号 + 逐字摘录`；每条摘录必须存在于对应证据的单一字段，不能跨字段拼接、跨 E 借用、生成 URL 或把 E 编号本身冒充原文。
- 最终答案、标题、采购人、发布日期、地域、阶段和链接全部由本地组装；模型输出无论多像答案，只要未知引用、额外字段、伪造摘录或 Markdown 连续两次不合规就整体回退，不保存原始响应。
- 问题中的未知 E 编号、改变角色、泄露系统提示/密钥/Cookie、执行代码或网络请求等提示注入在模型调用前拒绝；公告正文中的恶意指令既不能执行，也不能被选择为回答摘录。
- 当问题要求“全部 / 完整排名”但本轮证据超过单次有界上下文时，系统拒绝用部分证据冒充全量结论，并引导用户指定 E 编号、采购人或更窄主题。
- 未配置模型、模型超时或格式失败时，从固定快照本地抽取采购人、日期、阶段、编号、地域、附件或原文；运行中返回 409，损坏快照返回 `evidence_incomplete`，不会静默跳过后假装完整。
- 只缓存通过验证的 E 编号与原文选择，缓存键使用问题哈希、完整证据快照、上下文、模型端点和名称；不保存问题明文、聊天历史、模型原始响应、密钥或 Cookie，缓存读取仍重新校验证据。
- 使用用户已保存的 OpenAI-compatible 服务真实追问虚构公告“这个项目采购了什么，并给出原文依据？”，得到 `llm_grounded/applied`、1 条逐字引用和本地链接回填；11 项专项测试覆盖缓存、注入、未知/跨证据引用、一次修复、超时回退、完整性边界、重启恢复、快照损坏、运行状态与 API 错误码。

## 2026-07-18 — 决策闭环、机会工作台与买方雷达（逻辑单元 9）

- 决策中心完成网页企业画像读取、编辑、保存、重新读取和适配刷新；画像读取完成前字段保持禁用，避免网络慢时用空表单覆盖旧数据。每条结果展示建议参与/持续观察/暂不投入、适配分账本、风险、下一步、个性化影响和可点击证据。
- 反馈记忆完成相关/无关/观察/已联系的持久化、原因修改、撤销和全部清空；写入反馈与重新计算适配分拆开处理，重算失败不会误报“反馈未保存”。旧响应受 sequence 与运行 ID 约束，不能覆盖新运行或新反馈状态。
- 证据追问升级为 `run-qa-v3`：模型对结构化事实只能选择 `E 编号 + field`，采购人、标题、地域、日期、阶段和项目编号由本地固定快照回填；只有 excerpt 允许逐字摘录。真实复测“这批项目分别有哪些采购人”“分别来自哪些地区”“明确项目编号”均可回答。
- 修复多字段追问边界：“E01 的采购人和发布日期是什么”在采购人缺失但日期存在时，不再把整题误报为证据不足；系统返回已有日期并明确写出“采购人未在固定快照中识别”。确定性回退会区分未配置、模型不可用和模型选择未通过证据校验，不再误写“模型未参与”。
- 机会工作台新增面向零基础用户的“加入 → 分工 → 推进 → 收尾”说明和六个阶段解释；顶部拆成“我的机会”和“买方雷达”两个明确入口。卡片提供归档保留、恢复到待评估和二次确认删除，并在删除前逐项说明影响范围。
- `DELETE /api/v1/opportunities/{id}` 只删除 `opportunities` 工作台行。真实 API/浏览器验收确认：第一次点击不删除，第二次才执行；原始标讯、run_items、报告和反馈保持不变；同一真实标讯可重新加入并获得新 ID，人工字段从默认值重新开始。
- 新增买方雷达：只聚合本机 `tender_items`，按公告去重并展示采购单位识别覆盖率、公告/版本/估算项目数、生命周期、高频主题、来源和近期原文；不会联网补数、调用模型猜联系人或生成采购预测。
- 买方监控从本地 24 位稳定 `buyer_id` 创建，客户端不能提交或伪造 `buyer_keywords`；后续编辑、重启恢复和实际运行继续锁定采购单位，并复用统一检索、授权、调度、增量账本和投递链路。真实网页创建后在订阅中心可见，验收任务在关闭立即执行后清理。
- API 参考同步为 50 个 OpenAPI 操作；README、用户指南和零基础手册补齐机会归档/删除、买方雷达、数据边界和逐步操作。功能清单 F38、F40～F44 按实际验证结果标记通过。
- 真实浏览器桌面与 390×844 手机视口验收通过：七个底部导航和机会操作最小 44px，无页面横向溢出，买方雷达单列响应，控制台无产品 JavaScript 错误。独立终审发现并修复本地回退只覆盖前 5 条、模型可选择错误结构化字段和 DELETE 响应 Schema 缺失；最终 163 项 Pytest、Ruff lint/format、JavaScript 语法、`compileall`、JSON 与 `git diff --check` 通过。
- 运行时、HTTP User-Agent、手册生成器和 Python 包版本统一升级为 `0.7.0`；生成 `dist/bidpilot-0.7.0-py3-none-any.whl`（241,171 bytes，SHA-256 `f7b8e46508ed1884ba09702b03e42d974d99290bce03d85dd9d1601770e15e6d`），复验包含买方雷达与静态网页资源，不包含数据库或 secrets。

## 2026-07-18 — v0.7.0 发布并发加固（逻辑单元 10）

- 修复机会卡片 PATCH 与 DELETE 的两个并发窗口：删除先完成，或更新完成后、响应读取前发生删除，均稳定返回 404，不再由运行时断言泄漏为 500。
- 长期订阅的编辑、暂停、恢复和删除改为数据库单语句租约条件写入；worker 在用户操作期间新取得租约时，网页收到明确的 409 忙碌提示，任务、租约和原配置均保持不变。
- 手动立即运行在竞争中发现订阅已删除时返回 404，发现其他 worker 已领取时返回 409；订阅创建和重复创建路径不再以断言处理可恢复的跨进程状态变化。
- 新增 4 个确定性竞争回归场景，最终 167 项 Pytest、Ruff lint/format、JavaScript 语法、`compileall`、JSON/YAML、敏感密钥扫描、wheel 安装导入与 `git diff --check` 全部通过。
- 重新构建 `dist/bidpilot-0.7.0-py3-none-any.whl`（241,473 bytes，SHA-256 `29b12a3e7bb81ba474bf30887ae10d5011ae32bd53825cb4f87eb11b039b070e`）；包内共 48 个文件，确认包含最新租约保护、买方雷达与网页资源，不包含数据库、运行目录或 secrets。

## 2026-07-19 — v0.7.0 最终发布复核

- 默认服务端口固定复核为 `8000`；真实执行 `serve -> status -> restart -> status -> stop -> serve`，旧 PID 可优雅退出，新 PID 在 `http://127.0.0.1:8000` 恢复，`/health` 返回 200，`/openapi.json` 返回 50 个操作。
- 同步修复历史报告路径：`runs.report_path` 与 `reports.path` 统一到 `outputs/reports`，`delivery_attempts.message` 不再引用旧 `tmp/ui-v07`；数据库 `integrity_check=ok`，外键检查 0，34 条报告记录全部有文件，报告目录无孤立 DOCX。
- 私密文件权限加固落地：`data/secrets/control.token`、`runtime_config.key`、`source_auth.key` 在 Windows 上仅当前用户可读写；跨平台路径由 `bidpilot/private_files.py` 统一处理，权限无法收紧时失败而不静默降级。
- 零基础 Word 手册更新为 `outputs/manuals/标擎BidPilot零基础操作说明书_v0.7.0.docx`：46 页、8 张真实截图；通过 DocxToolkit 业务规则、a11y 审计 0 问题、Word 原生 PDF 导出与 46 页 PNG 视觉复核。
- 清理旧版交付物：移除 v0.5 手册、候选渲染目录、旧零结果截图、孤立报告、`tmp/`、`.pytest_cache/`、`.ruff_cache/` 和工作区 `__pycache__`；保留真实数据库、密钥、34 份数据库引用报告和最终 v0.7.0 手册。
- 最终工程门禁：`pytest` 172 passed、Ruff lint/format、Node `--check`、`compileall`、32 个 JSON、1 个 YAML、`pip check`、`git diff --check`、敏感密钥扫描、数据库/报告一致性检查全部通过。
- 重新构建 `dist/bidpilot-0.7.0-py3-none-any.whl`（243,597 bytes，SHA-256 `7afba6acdb4045ca427900a38a09652c1dc8a81955ea890df5b2f488500f739a`）；隔离安装后确认 `bidpilot.__version__ == 0.7.0`、OpenAPI 操作数 50、静态前端资源存在，wheel 不包含数据库、报告或 secrets。

## 2026-07-19 — v0.8.0 产品审计与验收契约

- 完成网页配置、来源授权、AI/检索、通知调度与安全边界的分离只读审计；确认当前 57 个 Settings 字段中 36 个已网页化，主要缺口是高价值检索参数、脏字段保存、来源恢复、revision 和配置来源说明。
- 定位千里马假成功/立即过期根因：旧登录入口、Cookie 扁平化、登录与检索环境切换、无账号探针、零结果误判和令牌轮换丢失；同时确认会员条款不允许把用户 Cookie 用于后台连续机器抓取。
- 定位多渠道的结构性缺口：API、模型、SQLite、UI 和调度均为单 `delivery_channel`，投递失败会整轮重抓且 ledger 不含 channel；v0.8 必须使用渠道级持久 outbox 与账本。
- 定位 AI P0：模型发现词可污染最终可信关键词、语义复核缺少逐字引句校验、行动数字可用子串绕过证据门、预览后执行会二次解析漂移。
- 追加 F45-F52 和 v0.8.0 Sprint 硬门，所有功能保持 `pending`，只有实际实现、运行验证、文档更新、提交和推送后才能改为通过。

## 2026-07-19 — AI 召回与证据约束加固（v0.8.0 逻辑单元 1）

- 将 LLM 扩展词明确标记为仅用于扩大候选召回，最终字面命中仍只接受用户主题与本地受控词，避免无关模型扩词直接放行公告。
- 模型查询变体必须关联已通过校验的检索词；被拒绝的变体不再执行，轨迹中的 `applied/rejected` 与真实来源调用保持一致。
- 语义复核必须同时返回本地可信概念与候选正文中的逐字证据引句；未知概念、伪造引句、置信度不足和超预算候选均保守拒绝并留下独立诊断。
- 情报简报按每条结论实际引用的同一 E 证据校验完整数字 token，阻止跨证据借数、`12` 冒充 `120`，并覆盖建议行动、优先机会、风险和采购需求中的数字。
- 逐公告摘要新增独立开关、数量、并发与正文字符预算；关闭时不调用模型，完整 OpenAI-compatible 端点不再重复拼接，摘要模式随记录持久传递。
- 48 项 AI/API 定向回归全部通过；Ruff lint、Ruff format 与 `git diff --check` 通过。F47 仍保持 `pending`，待配置网页化与完整端到端验收后统一标记完成。

## 2026-07-19 — 来源安全、状态诚实性与千里马合规模式（v0.8.0 逻辑单元 2）

- `HttpFetcher` 对 Cookie、Authorization 和 Proxy-Authorization 建立强制来源白名单：仅标准 HTTPS、允许域名和 443 端口可发送，跨域重定向、异常端口、回环、私网与保留地址在发出下一跳请求前拒绝。
- 来源聚合修复 `all(skipped) -> skipped`，混合跳过与可用结果诚实标记为部分覆盖；解析失败的发布日期使用固定旧哨兵，不再伪装成“今天”通过近期硬过滤。
- 千里马自动任务改为无需登录的公开招标/预告/中标/变更分类列表，支持有限真分页并保留公开原文入口；不再保存、迁移或后台重放会员 Cookie，不自动读取付费详情，来源中心提供用户主动打开原站入口。
- 中国招标投标网仍保留确实能增强公开详情的可见浏览器授权，但详情 Cookie 只允许发送到 `cecbid.org.cn` 白名单；授权完成没有有效域内 Cookie 时返回明确失败，网页不再无条件显示成功。
- 来源列表和健康趋势解耦加载，健康接口暂时失败不会让授权流程“打开浏览器后没有下文”；授权按钮在失败后恢复原文案，已保存但真实测试失败的状态不再显示为纯成功。
- 61 项来源、授权、安全、检索与 API 定向回归通过；真实在线读取千里马公开招标列表返回 `partial`、5 条公开候选与 5 个原站 URL，并以哨兵验证请求不消费会员 Cookie。F46 按实际证据标记通过。

## 2026-07-19 — 图形化配置治理与可信局域网（v0.8.0 逻辑单元 3）

- SQLite 运行时配置增加原子 revision 乐观锁、逐字段来源/更新时间/重启属性和普通字段恢复来源值；网页完整覆盖全部运行时白名单字段，逐卡只保存脏字段，多标签页冲突保留草稿，模型/通道测试不再隐式保存其他修改。
- 高价值检索、逐公告摘要、worker 轮询/租约/心跳、默认 8000 端口和 LAN 均可网页管理；敏感字段一一对应显式清除控件，读取失败时禁止保存，网络配置同时显示当前实际值和重启后值。
- 网络范围新增 `local/lan`，LAN 管理新增 `admin_token/trusted_lan`。令牌模式要求至少 16 字符；可信模式仅接受直连私网、链路本地和常见组网地址，拒绝公网 CIDR、全网段与伪造 `X-Forwarded-For`，但可信设备可完整使用查询、订阅、配置和来源授权写操作。
- 访问范围、LAN 策略、可信网段、管理员令牌和端口统一在重启后生效，解决 LAN→本机或策略切换在保存瞬间锁死远端页面的问题；CLI 拒绝仅本机模式用 `--host 0.0.0.0` 绕过配置，并在 LAN 启动时打印可尝试的跨平台局域网 URL。
- 移除不受内置浏览器支持的 `window.prompt()`，改为页面内“解锁局域网管理”面板：并发请求共用一次解锁、错误令牌可重新输入、令牌只进当前标签页 `sessionStorage`。远端来源授权不再额外要求回环地址，且明确可见 Chromium 在服务主机而非手机上打开。
- 使用隔离数据库真实监听 `0.0.0.0:8000`，通过 `192.168.1.6` 验证可信模式 GET/意图解析/编辑令牌为 200、来源授权请求到达业务层；切换令牌模式后重启，无令牌 403、正确令牌经图形面板解锁成功。保存前后策略保持一致，重启后一次性切换。
- 浏览器真实验收覆盖令牌生成、逐卡保存、当前/重启后提示、错误令牌重试和 390×844 响应式布局；修复手机保存按钮被压到 40px 宽的问题，最终宽 307px、高 44px、无横向溢出，控制台零错误。
- 40 项配置/API/来源授权/CLI 定向测试与全量 199 项 Pytest 通过；Ruff lint/format、`compileall`、Node 语法、JSON 解析和 `git diff --check` 通过。README、API 参考、配置指南、用户指南和零基础 Markdown 手册同步修正 LAN 与千里马授权边界。F45 与 F47 按真实证据标记通过；F51 继续等待多目标投递 UI。

## 2026-07-24 — 多目标可靠投递与持久 Outbox（v0.8.0 逻辑单元 4）

- 即时任务、普通订阅和买方监控统一支持 1～10 个 `delivery_targets`；旧 `delivery_channel` 请求、响应和 SQLite 数据继续兼容，旧总体账本安全迁移到原单一目标，不会在升级首轮重发历史公告。
- 每个目标独立计算尚未确认的公告集合；失败目标的待处理公告被 Outbox 占位，后续调度不会为同一目标重复排队，成功目标不会收到为其他失败目标补发的旧内容。目标集合不一致时生成独立 Word 报告，运行级报告仍保留全部本轮新增证据。
- 报告记录、全部目标 Outbox 和目标公告映射在首次外发前一次事务落库；运行也在外发前持久化为可恢复状态。worker 重启后可继续 pending/retrying，成功状态与目标级账本同一事务提交，过期租约允许接管而旧 lease token 无法覆盖新所有者。
- 单目标失败不再让整轮重新抓取：运行返回 `partial`，仅失败目标按 60 秒起的有界退避重试；第五次失败进入 `dead_letter`。新增逐目标队列查询和单死信重试 API，回执直接返回 `outbox_id`，原报告缺失会立即进入死信而不谎报成功。
- 编辑订阅移除目标时，原 pending/retrying/dead_letter 原子取消并保留审计；目标正在真实外发时拒绝修改或删除，避免页面显示取消但外部仍收到。删除订阅会明确取消尚未发送的 Outbox。
- 明确外部通道为 `at-least-once`：已被 SQLite 确认的成功目标不会重复，但外部平台已接收、进程在本地确认前崩溃的极小窗口无法由普通 Webhook/SMTP 统一消除，API 文档不再宣称端到端 exactly-once。
- 新增旧库迁移、双目标全成功、局部失败、仅失败目标重试、待重试占位、目标移除、发送中保护、报告先持久化、重启恢复、租约栅栏、五次死信、手动恢复、报告丢失和 API 脱敏测试。全量 211 项 Pytest、Ruff lint/format、`compileall`、Node 语法和 `git diff --check` 通过，F48 与 F49 标记通过。

## 2026-08-02 — v0.8.0 最终发布收尾

- 完成意图签名快照、订阅编辑快照校验和买方关键词本地锁定；修复 worker 租约接管竞态、旧 worker 外发窗口、IPv6/显式 LAN 地址停止控制和旧运行记录清理。
- 配置中心覆盖 62 个白名单字段并提供中文帮助，OpenAPI 共 52 个中文操作；Telegram、Slack、报告发布、多目标选择和 Outbox 控制塔在桌面与 390px 手机视口完成真实浏览器验收，控制台无产品错误。
- 全量回归在最终代码状态达到 275 passed；发布收尾按用户要求不重复慢速全量，复跑 26 项快照/控制/生命周期高风险测试，并通过 Ruff、JavaScript 语法、`compileall`、`pip check`、JSON/YAML/TOML、SQLite integrity/foreign keys、OpenAPI 与配置说明契约。
- v0.8.0 零基础操作说明书更新为 59 页、10 张真实截图、7 张表格；DOCX 业务校验通过，Word 原生 PDF 与全部 59 页渲染视觉复核通过。新增面向首次接手人员的 Markdown 交接手册，覆盖安装、8000 端口、停止、LAN、LLM、渠道、备份、升级和排障。
- 重新构建 `dist/bidpilot-0.8.0-py3-none-any.whl`（300,596 bytes，SHA-256 `0910e8d5e349ff8847bfd6d9f6b6446993523bbb970b6cef212fc5c90bfa9bec`）；51 个包内文件完整，隔离安装确认版本 0.8.0，未包含数据库、报告、Cookie、密钥或 `.env`。
- 清理 v0.7.0 wheel/说明书、旧零结果截图、被 Telegram/Slack/Outbox 新截图替代的旧图和文档审计临时产物。Docker CLI 在本机不可用，因此真实 Compose 构建仍须由有 Docker 的环境执行。
- F50、F51、F52 仅更新状态为 `passing`，保留 append-only 描述不变；发布提交、`v0.8.0` 标签、全部分支和标签将同步到 GitHub 并以远程引用复核。

## 2026-08-02 — 登录来源真实检索修复与比赛交付收口

- 中国招标投标网适配器跟随站点现行搜索契约：移除会导致零召回的单一 `tenders` 过滤，改用当前全部内容检索并继续执行本地日期、地域、公告类型和主题硬过滤；会话同时发送到搜索与详情请求，且始终受 HTTPS 与来源域名白名单保护。
- 授权生命周期拆分为“捕获未验证 / 验证通过 / 无法判定 / 验证失败 / 过期”；捕获任意域内 Cookie 不再等于登录成功，失败和无法判定的会话不会加载到后台检索。完成授权后网页自动执行多探针真实验证，零候选返回 `inconclusive` 而不是误报登录失败。
- 详情门禁识别适配当前拆分文本结构、登录重定向和登录表单；只有真实详情正文解锁时才标记 `free_member`。真实站点使用本机加密会话完成搜索并读取 1 条免费会员详情，状态为 `passed/authorized`，最长验证信任期收敛为 7 天。
- 千里马继续维持合规公开分类与用户主动打开原站模式。其官方注册条款明确禁止非人为连续、频繁查询以及机器抓取、复制、镜像，因此不新增后台 Cookie 重放；命题的登录来源硬要求由已验证的中国招标投标网满足。
- 新增或更新 32 项授权、来源和敏感请求定向回归；Ruff 与真实网络验证通过。CLI 和授权烟测默认来源统一为 `cecbid`，移除已废弃的千里马 Cookie 环境变量与配置入口，README、API、用户、配置和零基础文档同步更新。
- 功能清单追加 F53，并以真实会员搜索、详情解锁和自动化回归证据标记通过。

## 2026-08-02 — 千里马持久前台免费会员检索

- 用户明确要求在 BidPilot 内保持本人免费账号登录态并直接检索账号原本可见的免费列表，而不是仅跳转原站；同时继续禁止伪造登录、Cookie HTTP 重放和付费权限绕过。
- 使用真实已登录页面验证“服务器”搜索：原站返回 137076 条匹配，首屏可见 20 条真实标题、日期、阶段、地域和原文链接。
- 千里马授权改为独立可见 Chromium 配置与同一浏览器进程复用：用户本人登录，系统不导出 Cookie；完成授权后保持该进程，由真实验证和即时查询继续使用。
- 会员增强只允许 `immediate` 即时任务、单个用户主题、首屏最多 20 条和 10 秒冷却；定时任务继续使用公开分类流，代码不翻页、不点击详情、不下载附件或读取付费内容。
- 新增首屏结构解析、持久浏览器状态和清理回归；旧版千里马明文/加密 Cookie 仍会删除且不迁移。
- 真实重新登录验证通过：来源中心显示“授权已验证可用”，验证词“服务器”首屏读取 20 条免费会员列表候选；未翻页、未读取付费详情、未导出或后台重放 Cookie。
- 随后通过普通网页任务运行 `8eb35f8c2747410c8d564ecb4fb8cf9a` 再次验证：千里马扫描/读取 20 条、通过本地时间硬校验保留 3 条，3 条落库记录均标记 `auth_level=free_member`，原文链接为 qianlima.com；报告保存为 `outputs/reports/服务器招标信息_202608021338_8eb35f8c.docx`。功能清单 F54 据此标记通过。

## 2026-08-02 — 跨平台后台运行修复（v0.8.1 候选）

- GitHub Actions 失败根因确认为 Rich/Typer 在 POSIX 终端插入 ANSI 样式，导致安全拒绝测试对裸 `--host` 的平台相关误报；业务拒绝行为本身正常。测试现统一剥离 ANSI 后再核验用户可见语义。
- 千里马首次授权仍强制使用用户可见图形会话；已验证的持久浏览器配置可在 Linux 无显示后台服务中以 headless Chromium 执行一次用户主动首屏查询，不导出 Cookie、不交给 HTTP 客户端、不定时、不翻页、不访问付费详情。
- 新增 `auto/visible/headless` 浏览器运行策略：Windows/macOS 与 Linux 桌面默认可见，Linux 无 DISPLAY/WAYLAND_DISPLAY 时自动无头；显式 visible 在无图形会话中失败关闭并返回可操作说明。
- 新增 hardened systemd web/worker 单元与生产环境样例；保留 Docker Compose 双服务、健康检查、回环端口发布和优雅停止。CI 升级至 Actions v7，新增格式、compileall、结构化配置、Compose 模型与 Ubuntu 镜像构建检查。
- Windows 最终代码状态完成 287 项 Pytest，Ruff lint/format、compileall、JavaScript、JSON/YAML/TOML 和 `git diff --check` 全部通过；Linux WSL 与 GitHub Actions 结果将在同一交付批次继续记录。
