# Progress

## 2026-07-17 — 立项与证据流水线

- 初始化 `main` 仓库，定义产品名“标擎 BidPilot”和团队名“聚标成擎”。
- 核验超聚变命题、报名表、赛制、截止时间和交付要求。
- 完成中文查询编译器、Pydantic 领域模型、SQLite 基础结构。
- 完成中国招标投标网、中国政府采购网、千里马授权源骨架。
- 完成严格筛选、证据摘要、转载去重、生命周期聚类、机会评分和 DOCX 报告。
- 完成 FastAPI、CLI 和第一版 Web 闭环。

## 2026-07-18 — 真实工程化里程碑

### 来源

- 来源从 3 个扩展到 5 个，新增全国公共资源交易平台、商务部中国国际招标网。
- 全国平台只使用首页最新公告流，始终标记 `partial`，不冒充全量历史检索。
- 商务部适配器使用真实表单接口，已完成在线查询、详情解析和证据保留。
- 千里马未授权时返回 `auth_required`，等待用户主动登录免费会员。

### 长期任务

- 移除 APScheduler 进程内闹钟，改为 SQLite 持久 worker。
- 支持 `python -m bidpilot worker` 和 Web 内嵌 worker。
- 持久化 `next_run_at`、运行、投递尝试、失败次数、worker 心跳、租约和增量账本。
- 实现重启补跑、失败退避、多 worker 原子领取、长任务续租和手动/自动执行互斥。
- 故障注入验证：投递失败不写账本；首次重试约 60 秒；租约未过期不能重复领取；过期后可接管。

### 投递与管理

- 本地报告中心明确标记“不主动推送”。
- 飞书 Webhook 支持运行卡片和下载链接；飞书应用支持 Word 文件与无新增文本回执。
- 新增跨平台标准 SMTP：SSL/STARTTLS、Word 附件、多收件人、无新增回执。
- 未配置渠道在 UI 禁用，创建订阅时拒绝虚假推送承诺。
- 订阅中心支持编辑自然语言规则、通知策略、渠道、暂停、恢复、立即执行、日志和二次确认删除。
- 新增来源中心：官方/行业属性、接入模式、授权状态、最近检查、抓取/保留和诊断。
- 修复 1280px 下来源浮层覆盖查询面板；编辑期间自动刷新不再清空输入。

### 跨平台与部署

- 删除 `setup.ps1`，新增纯 Python `bootstrap.py`。
- 新增非 root Dockerfile、Compose Web + 独立 worker、持久卷、健康检查和自动重启。
- Compose 补齐 LLM、会员会话、飞书和 SMTP 环境变量。
- 新增 Windows/macOS/Linux、Python 3.11/3.13 GitHub Actions 矩阵。
- 本机没有 Docker，不能把镜像构建标记为已验证。

### 真实验收证据

- 订阅 `f8b24edfbe64481dbcc7d4fdee85d94f` 在真实进程重启前后保持同一 ID、`next_run_at`、运行历史和投递历史。
- 首轮真实查询“最近1个月江苏芯板招标信息，请每天9:00发送给我”新增 5 条并生成 DOCX。
- 浏览器点击第二轮“立即执行”新增 0 条，仍记录本地运行回执；运行数 2、投递尝试 2、账本仍为 5。
- 浏览器完成临时订阅的规则编辑、计划重算、暂停、恢复和二次确认删除。
- 来源中心真实显示 5 个来源，其中 4 个当前可运行、3 个官方平台；千里马待授权。
- 最终门禁：36 项 Pytest、Ruff lint/format、JavaScript 语法、JSON/YAML、依赖完整性、敏感令牌扫描和 wheel 构建全部通过。
- wheel 构建门禁曾发现静态资源重复收录，已移除 Hatch 重复配置；最终生成 `bidpilot-0.2.0-py3-none-any.whl`。

## 2026-07-18 — 机会工作台里程碑

- 新增项目级 `opportunities` 持久表与 REST API，只允许从 `tender_items` 的真实标讯创建机会。
- 同一 `project_key` 重复加入保持同一机会 ID，不重复生成线索，也不改动已稳定的人工状态。
- Web 新增机会工作台：阶段看板、搜索、筛选、负责人、下一步、标签、备注、已读状态和归档管理。
- 生命周期时间线保留同项目全部已抓取事件与原文链接；同日事件按采购意向、招标、更正、中标、合同阶段择新。
- 后续真实生命周期事件自动刷新机会快照并重新标为未读，同时保留阶段、负责人、待办、标签和备注。
- 真实浏览器使用“南通深南电路有限公司项目-芯板立体库”完成加入、编辑、搜索、筛选、已读和生命周期闭环；时间线包含招标与更正两个真实来源事件。
- 两次真实进程重启后，同一机会 ID、跟进中阶段、负责人、下一步、标签和备注均保持；最新更正公告正确成为卡片快照。
- 筛选无结果提供一键清空操作，空工作台明确引导从真实检索加入，不使用演示假数据。
- 最终门禁：38 项 Pytest、Ruff lint/format、JavaScript 语法、JSON/YAML、依赖完整性、OpenAPI、令牌扫描、`git diff --check` 和 wheel 内容检查全部通过。
- 生成 `bidpilot-0.3.0-py3-none-any.whl`（81,647 bytes，SHA-256 `b5c9b1a8fc7845cc928139410c3e44630937a00d35a784d59a862b8d34745b54`）；静态 Web 资源与 API 代码已在包内复验。

## 当前约束

- 仓库没有 Git remote；可以创建本地原子提交，但无法满足 `push` 要求。
- 本机没有 Docker；需要在有 Docker 的环境实际执行 `docker compose up -d --build`。
- 飞书真实投递需要用户提供 Webhook 或应用凭据；SMTP 真实投递需要应用专用密码。
- 千里马真实链路需要用户主动完成免费会员登录，Cookie 不得提交仓库。
- 比赛材料和最终表单提交按用户要求暂缓；任何对外提交仍需动作时确认。

## 2026-07-18 — 可配置智能中枢与解析器 v2

- 修复截图原句“最近1个月深圳充电桩招标信息”：主题为“充电桩”、地域为“深圳”、来源地域代码为 `440000`，并在真实浏览器完成点击验收。
- 意图解析器升级为 `rules-v2`：支持常见城市、自治区全称、中文数字时间窗、半年/季度/本周/上周/今天/昨天、中文时钟、每日/每周/每月/月末/一次性计划、排除词和显式公告类型。
- 普通“招标信息/采购信息”不再错误限制为招标公告，完整保留更正与中标生命周期；明确“只看招标公告”等表达仍严格过滤。
- 字段级意图矩阵扩展到 30 条以上，覆盖截图原句、否定公告类型、月底计划与多种通道；相关解析、生命周期和调度回归通过。
- 新增网页配置中心：OpenAI-compatible 模型、飞书群机器人/应用、SMTP、钉钉、企业微信、通用 Webhook 均可网页管理，保存立即生效并在进程重启后恢复。
- 配置写入只接受白名单字段并要求短期编辑令牌；敏感值使用本机 Fernet 密钥加密后存入 SQLite，读取 API、状态、日志与错误只显示配置状态。
- 使用用户提供的本机 OpenAI-compatible 服务完成真实模型连通测试，模型回复正确，密钥未进入仓库、文档、日志或 API 响应。
- 新增钉钉 HMAC 加签、企业微信 Markdown 和通用 JSON Webhook；HTTP/SMTP 错误统一脱敏，避免含 token 的 URL 进入运行日志。
- `/docs` 的 29 个 API 操作全部增加中文用途、参数、返回、副作用、错误码和示例，并新增 API、配置、渠道、用户四份独立文档。
- 最终门禁：85 项 Pytest、Ruff lint/format、JavaScript 语法、JSON/YAML、依赖完整性、OpenAPI 文档契约、敏感模式扫描、`git diff --check` 和 wheel 构建全部通过。
- 生成 `bidpilot-0.4.0-py3-none-any.whl`（105,842 bytes，SHA-256 `89e262186409f4eede30a98d651124c43ed9a7418c690b7df502fba47424e7fb`），复验包含配置中枢与静态 Web 资源。

## 2026-07-18 — v0.5.0 混合意图、可解释检索与零基础交付

- 混合意图引擎升级为 `hybrid-v1`：`rules-v2` 始终先生成可执行基线，仅低置信、缺失或冲突字段才允许 LLM 提议；严格 JSON、地域、日期、计划、枚举、字段锁定和逐字段决策共同约束合并。
- 浏览器复测“最近1个月深圳充电桩招标信息”，稳定得到主题“充电桩”、地域“深圳”、近 1 个月和立即执行；规则高置信时明确显示“无需调用 LLM”。
- 零结果不再只显示“0 条”：新增扫描 → 候选 → 保留漏斗、逐项排除原因、来源覆盖缺口和最多三条不会自动执行的安全放宽建议；即时任务为 0 条时仍生成 Word 诊断报告。
- 网页配置中心覆盖 OpenAI-compatible 模型与飞书、SMTP、钉钉、企业微信、通用 Webhook；敏感值加密保存且 API 只返回配置状态，网页可保存、清除和真实测试，无需手改配置文件。
- `serve/status/stop/restart` 形成完整生命周期控制；真实优雅停服和新进程启动后，订阅 ID、启用状态、`next_run_at`、投递策略及脱敏模型配置逐字段一致，内嵌 worker 自动恢复在线。
- OpenAPI 共 31 个操作，全部具备中文用途、参数、返回值、副作用、常见错误和示例；Swagger 与手册统一为最终解析器 `hybrid-v1`、规则基线 `rules-v2`。
- 新增可复现构建器 `tools/build_beginner_manual.py`，生成 43 页《标擎 BidPilot 零基础操作说明书》：真实目录页码、4 张实机截图、跨平台安装、启动/停止/重启、订阅、模型与通道、备份恢复、故障树、术语表和命令速查完整覆盖。
- Word 说明书通过业务规则校验、Word 原生 PDF 导出、43 页逐页视觉复核、标题/章节/字段/图片/表格/可访问性审计；无裁切、重叠、孤立尾页或大片异常空白。简化 XSD 对关系命名空间的误报已由业务校验与 Word 原生打开双路径替代验证。
- 最终工程门禁：97 项 Pytest、Ruff lint/format、JavaScript 语法、JSON/YAML、依赖完整性、OpenAPI 合约、敏感凭据扫描、`compileall`、`git diff --check` 和 wheel 内容检查通过。
- 生成 `bidpilot-0.5.0-py3-none-any.whl`（131,674 bytes，SHA-256 `d7f16174830101b38157cbf545534a263ca94e832142fb58ffaa23f4035e9db1`），确认包含混合意图、控制面与静态 Web 资源且不包含运行数据或密钥。

## 下一步产品优先级

1. 采购单位/竞争对手监控与高级组合规则。
2. 命中原因解释、可配置机会评分和机会待办日历。
3. 来源健康历史和适配器回归监测。
4. 核心产品稳定后再生成报名资料、详设、操作手册、PPT/PDF 和演示视频脚本。

## 2026-07-18 — v0.6.0 AI 深度检索重构启动

- 新增 v0.6 硬性验收契约：检索计划、多轮补搜、语义边界复核、至少 8 个真实来源、统一网页授权、固定召回评测与完整安全门禁。
- 将 F27–F32 追加到功能清单，保持 append-only；在真实实现和验收完成前全部保持 `pending`。
- 建立无业务写入的 `tools/evaluate_retrieval.py` 固定评测器；强制关闭模型，不写 SQLite、不生成报告、不触发投递，只记录真实扫描、候选、保留、证据 URL、来源诊断与排除原因。
- 2026-07-18 15:03 基线实测四类查询共扫描 1,258 条、进入统一过滤 44 条、最终保留 2 条、对应 4 个证据 URL；深圳充电桩与江苏数据中心均为 0 条，北京医疗设备和全国服务器各保留 1 条。
- 基线证实当前瓶颈：CCGP 两页固定公告流共贡献大部分扫描但缺少站内关键词召回；CEC 搜索结果多数超出时间；GGZY 仅首页流；Mofcom 稀疏；千里马未授权。后续新版必须使用同一评测器对照，不以主观观感宣称提升。
- 安全边界保持不变：只使用公开入口、合理缓存/限速和用户主动授权会话；不破解验证码、CA、付费墙、账号权限、访问控制或频率限制。

### AI 检索内核（逻辑单元 1）

- 新增 `retrieval-v1` 检索计划：确定性同义词与本地行业词典始终可用；模型仅用严格 JSON 提出词条、查询变体和来源优先级，通用词、日期、URL、地域冒充主题、低置信与控制语句均在本地拒绝。
- 使用已保存的 OpenAI-compatible 服务完成真实规划调用；“北京医疗设备”在本地 6 个概念基础上增加医用呼吸机、医学影像设备、生命监护仪等 6 个经校验词条，模型响应和密钥均未持久化到运行轨迹。
- 流水线升级为最多两轮：所有来源先跑核心主题；匹配候选少于 5 条时，只对声明支持关键词变体且首轮可用的来源执行一次补搜，固定列表/首页流不重复扫描，来源 URL 在摘要前去重。
- 时间、地域、公告类型和排除词拆成不可覆盖的硬过滤；仅硬过滤通过但无字面命中的候选进入批量语义复核，接受阈值默认 0.82，模型缺失、超时、格式错误或漏回候选时均保守拒绝。
- 改进同义词相关度：受控同义词命中标题/正文获得明确语义权重，避免“有同义词命中却因原主题不在标题而低于 45 分”的二次误杀。
- `RunResult` 与 SQLite 新增向后兼容 `retrieval_json`：持久保存计划、每轮查询/调用/扫描/候选/缺口、语义决策与总览，不保存模型原文、密钥或 Cookie。
- 5 个既有适配器补充稳定来源 ID、公开/会员模式、查询方式、分页/地域/日期/详情能力与真实覆盖说明，为补搜调度和图形化来源中心提供统一元数据。
- 新增 5 项检索专项测试，覆盖本地扩词、固定源不重复、严格 JSON 校验、模型格式回退、语义接受和地域硬过滤不可越权；完整 102 项 Pytest 与 Ruff 通过。
- F27–F29 已在功能清单中按实际验证证据标记通过：真实模型规划、无模型回退、两轮补搜、URL 去重、地域硬过滤与语义复核均有自动化测试或在线轨迹支撑。

### 官方来源扩展（逻辑单元 2）

- 来源从 5 个扩展到 8 个，新增中央政府采购网、军队采购网和中国招标投标公共服务平台；三者均具备独立适配器、稳定来源 ID、能力元数据、解析夹具和失败隔离。
- 真实在线烟测“最近1个月全国服务器采购信息”：公共服务平台返回 20 条公开列表摘要并诚实标记 `partial`；军队采购网返回 20 条公开正文并标记 `ok`；中央政府采购网返回 20 条电子卖场公告但详情受网关限制，因此标记 `partial`。
- 不穿透公共服务平台的 WAF/VAPTCHA，不绕过中央政府采购电子卖场详情网关，不代替用户完成军队采购工作台登录或 CA；受限范围全部写入来源能力说明和运行诊断。
- 新增 `tools/smoke_sources.py` 与 `outputs/evaluations/source-smoke-v0.6.json` 作为可复现在线证据；完整 102 项 Pytest、Ruff lint/format 与 `git diff --check` 通过。
