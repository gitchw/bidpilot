# 标擎 BidPilot 总体验收契约

## 硬性通过条件

1. 四条赛题示例和至少六条边界表达均能解析出正确的主题、地域、时间范围和计划；时区固定为 Asia/Shanghai。
2. 在线运行至少访问两个不同招投标网站；登录源未授权时清晰提示，授权测试夹具能证明其解析链路完整。
3. 每条保留结果均满足时间、地域和主题条件，并包含标题、发布时间、来源 URL、核心内容与附件列表。
4. 同一公告跨站转载只保留一个主记录并保留全部来源；同一项目的更正或中标不被误删。
5. 首次订阅成功确认投递 N 条后，在来源不变时再次执行新增为 0 且账本数量不增长；投递失败不写账本，同项目新版本可再次提醒。
6. 立即任务生成符合 `{用户问题}_{yyyyMMddHHmm}.docx` 的可打开报告；报告含查询口径、执行摘要、来源覆盖、结果明细和证据说明。
7. Web UI 可完成“输入 -> 解析 -> 执行 -> 查看结果 -> 下载 Word -> 创建订阅”的闭环，并支持编辑规则、暂停、恢复、立即执行、日志和二次确认删除；API 与 CLI 可完成同等核心流程。
8. 任一来源超时、登录过期或结构变化不会导致全任务崩溃；报告披露部分失败与覆盖边界。
9. 无 API 密钥时系统仍可运行抽取式摘要；有兼容 LLM 配置时启用证据约束摘要且验证失败自动降级。
10. 测试套件、静态检查、端到端冒烟测试全部通过，最终交付文件经渲染目检无明显版式缺陷。
11. 真实进程重启后订阅、下次时间、运行历史和投递账本不丢失；两个 worker 不能同时领取，长任务执行期间持续续租。

## 评委维度与目标分

- AI 创新性（目标 95/100）：查询编译、证据门控摘要、生命周期图、可解释置信度。
- 业务价值（目标 95/100）：减少检索与筛选时间、避免重复触达、突出可行动商机。
- 可推广性（目标 92/100）：来源插件、模型插件、渠道插件和部署无关设计。
- 方案专业度（目标 96/100）：真实运行、错误降级、可观测性、合规与完整交付证据链。

## 失败判定

- 任何核心结果被硬编码。
- 仅有界面或演示稿，没有可运行抓取链路。
- 报告事实无法回指来源，或在来源失败时伪造完整结果。
- 定时任务重复推送既有内容。
- 最终文档、代码或演示缺少命题要求的任一组成部分。

## Sprint：机会工作台

硬性通过条件：

1. 只能把 `tender_items` 中已有的真实记录加入机会，客户端不能提交一份伪造标讯快照。
2. 同一 `project_key` 重复收藏返回同一机会，不创建重复销售线索。
3. 机会可持久保存负责人、阶段、下一步时间、备注、标签和已读状态；非法阶段返回 422。
4. 生命周期接口返回同一项目的全部已抓取事件，按发布时间排序，并保留每个事件的来源链接。
5. Web 可从真实结果卡加入机会，并在机会工作台完成阶段、负责人、下一步、备注和归档管理。
6. 进程重启后机会状态不丢失；空工作台有清晰引导，不使用演示假数据填充。
7. API 单测、浏览器闭环、完整回归、lint/format、wheel 构建全部通过。

## Sprint：可配置智能中枢与中文开发者体验

硬性通过条件：

1. 意图解析样例矩阵不少于 25 条，覆盖数字/中文数字时间窗、绝对日期区间、今年/半年/季度、今天/昨天、全国与常见城市、早晚/半点、每日/每周/每月/一次性计划、排除词、公告类型和投递渠道；每条均以字段级断言验证，不以人工观感代替测试。
2. 解析出的主题不得混入“帮我、过去、每天、推送、发送到飞书”等控制语句；显式全国不产生“未识别地域”警告；城市检索保留城市粒度并映射到所属省级来源代码。
3. `TenderQuerySpec` 支持排除词和公告类型过滤，过滤逻辑实际进入规范化流水线；月度计划可计算严格晚于当前时刻的下一次执行时间。
4. Web 新增“配置中心”，可直接管理兼容模型 API 地址、模型名和密钥，以及飞书、邮件、钉钉、企业微信和通用 Webhook 渠道；保存后立即生效且真实进程重启后仍保留，无需手改 `.env`。
5. 配置 API 只接受显式白名单字段；密钥、密码、Webhook 完整地址和令牌不得在读取接口、系统状态、日志或错误中回显；留空默认保持已有密钥，必须显式操作才能清除。
6. 配置写入需携带同源页面取得的短期编辑令牌；服务继续默认监听本机，并明确提示公网部署必须增加 TLS 与身份认证。
7. 模型连接测试调用用户配置的 OpenAI-compatible `/chat/completions` 接口并返回可诊断但脱敏的结果；无配置时继续使用证据抽取摘要，模型失败自动降级。
8. 飞书、SMTP、钉钉、企业微信和通用 Webhook 均提供配置状态；新增渠道使用各平台公开或用户授权的标准接口、有限超时和诊断错误，不绕过验证码、付费墙、权限、频率限制或站点访问控制。
9. `/docs` 中每个业务 API 都有中文标签、用途、参数、请求示例、返回说明、可能副作用和常见错误码；配置测试、立即执行、删除等有外部或不可逆副作用的接口必须醒目标注。
10. 交付文档拆分为 API 参考、配置指南、投递渠道指南和用户操作说明；命令、字段、边界和故障排查与实际代码一致，不再以几行 README 代替完整说明。
11. 临时数据库单测、真实浏览器配置页与 Swagger 验收、完整 Pytest、Ruff、JavaScript、JSON/YAML、敏感信息扫描、wheel 构建和重启持久化全部通过。

## Sprint：混合意图引擎与零基础可运维交付（v0.5.0）

硬性通过条件：

1. 任何查询都先由确定性规则生成基线；仅在 `auto` 模式的低置信/缺失/冲突场景或 `always` 模式调用 LLM，`off` 模式绝不发送模型请求。
2. LLM 只能返回严格 JSON 提议。多余字段、Markdown 包裹、非法枚举、非法日期、未知地域、未在原问题出现的主题/排除词以及不完整计划一律拒绝，不得直接覆盖业务模型。
3. 地域名称必须由本地行政区词表解析，`region_code` 与地域层级只能由本地代码派生；LLM 提供的代码不得被信任或保存。
4. 仅低置信或发生冲突的字段可被修正；高置信规则字段默认锁定。模型超时、HTTP 错误、无配置、响应格式错误或全部提议被拒绝时，返回可执行的规则结果且不让任务失败。
5. 返回值必须披露规则/混合模式、模型状态、触发原因、字段级接受/拒绝理由和耗时，但不得披露 API Key、完整授权地址或模型原始响应。
6. 查询执行、API 意图解析、网页创建/编辑订阅和 CLI 解析全部接入同一混合引擎；保留同步规则方法以兼容已有内部调用和历史数据。
7. Web 配置中心可图形化设置 `off/auto/always` 与置信阈值，并明确告知云模型会接收自然语言问题；意图面板直观显示“规则直接通过”“LLM 辅助修正”或“安全回退”及字段决策。
8. 提供意图对比 API，一次返回规则基线、最终结果和安全决策轨迹；API 文档逐接口包含中文用途、参数、返回、副作用、错误和可复制示例。
9. 提供跨平台 `serve`、`status`、`stop`、`restart` 命令。停止命令通过本机控制令牌请求当前服务优雅退出，不盲杀 PID；前台服务始终可用 `Ctrl+C` 停止。
10. 零基础说明书从“什么是终端/localhost”开始，逐步覆盖安装、启动、状态、停止、重启、Docker、首次查询、模型配置、订阅、推送、备份恢复、排错、卸载和安全边界，所有命令与 v0.5.0 实现一致。
11. 真实模型至少验证“帮我查最近45天泉州储能系统项目”一类规则易混淆表达；自动化测试覆盖应用、拒绝、回退、模式、配置持久化和控制令牌，且不使用真实密钥作为测试夹具。
12. 完整 Pytest、Ruff、JavaScript、OpenAPI、JSON/YAML、秘密扫描、wheel 构建、真实浏览器和 DOCX 逐页渲染门禁全部通过；现有数据库、订阅、机会和加密配置保持可读。
13. 零结果页必须用中文展示“扫描 → 候选 → 保留”漏斗、逐项排除原因、来源覆盖缺口和最多三条安全放宽建议；建议按钮只能填回输入框，不能自动发起查询。

## Sprint：AI 深度检索、多轮补搜与统一来源中心（v0.6.0）

硬性通过条件：

1. 每次运行都生成可审计的检索计划，至少包含原始主题、受控同义词、行业术语、查询变体、来源选择和预算；未配置模型或模型失败时必须生成确定性的本地计划，任务不得失败。
2. LLM 只允许输出严格 JSON 的检索建议和候选相关性判断，不得生成公告、URL、日期、采购单位或金额；所有词条必须通过本地长度、数量、去重、通用词、地域、日期和枚举校验后才能使用。
3. 第一轮候选稀疏、主题字面命中不足或来源覆盖缺口明显时，系统自动执行至多一轮补搜；固定首页流不得重复扫描，支持关键词检索的来源按查询变体补搜，且同一来源/URL 不得重复下载。
4. 时间、地域、公告类型和排除词始终是硬过滤；只有通过这些硬过滤、但没有主题字面命中的边界候选可进入批量语义复核。模型接受或拒绝必须记录置信度、证据片段和中文理由，模型不可用时保守拒绝。
5. 固定评测集至少包含“深圳充电桩、北京医疗设备、江苏数据中心、全国服务器”四类真实查询；与 v0.5 单轮原词基线相比，新版候选 URL 去重后召回不得下降，至少三类查询的候选数或可信保留数提升，且所有保留结果均能打开原始来源。
6. 来源从 5 个扩展到至少 8 个真实适配器，优先覆盖中央政府采购网、军队采购网、中国招标投标公共服务平台；每个适配器必须有解析夹具、失败隔离、能力说明和真实在线冒烟证据，不稳定入口只能标记 `partial`，不得宣称完整覆盖。
7. 全国公共资源交易平台应优先使用其公开交易查询入口并支持关键词、日期和地域条件；如果官方入口变化或限制访问，必须安全降级并在来源中心披露实际能力，不回退为“假装全量”的首页流。
8. 来源中心必须图形化显示公开/授权模式、支持的查询能力、轮次、最近扫描/候选/保留、响应耗时、授权状态和覆盖限制；用户无需编辑 `.env` 或复制 Cookie 即可发起支持来源的本机授权流程。
9. 统一授权只能打开可见浏览器，由用户亲自登录、完成验证码或 CA；用户明确点击完成后才读取临时浏览器上下文 Cookie，加密写入 SQLite，不保存账号密码，不自动识别验证码，不绕过付费墙、角色权限、访问控制或频率限制。
10. 授权流程必须具备一次性会话 ID、回环地址限制、短期编辑令牌、进行中/已完成/失败/过期状态、测试和清除操作；API、日志、错误、截图及来源状态永不回显 Cookie 原文。
11. Web 结果页必须展示“AI 查询计划 → 第 1 轮召回 → 缺口判断 → 第 2 轮补搜 → 硬过滤 → AI 边界复核 → 去重保留”的真实数据，并解释每个扩展词和补搜原因；用户可关闭 AI 规划或语义复核，关闭后仍能完成确定性检索。
12. API 与零基础说明书逐字段解释检索计划、轮次诊断、语义决策和授权状态；从安装浏览器依赖、开始授权、完成授权、测试、清除、启动、停止、重启到故障排查均给出可复制步骤。
13. 固定夹具测试、模型伪响应测试、模型失败回退、真实在线召回对照、真实浏览器操作、完整 Pytest、Ruff、JavaScript、OpenAPI、秘密扫描、`compileall`、`git diff --check` 和 wheel 构建全部通过；已有数据库、订阅、报告、机会和加密配置保持兼容。
