# 标擎 BidPilot：可验证招投标情报 Agent

## 1. 项目目标

面向超聚变“招投标信息聚合工具”命题，交付一个真实可运行、可验证、可持续订阅的 Web/API/CLI 系统。用户只需输入自然语言，系统即可解析主题、地域、时间与频率，从多个招投标站点采集信息，清洗、筛选、跨站去重并生成 Word 报告；有频率要求时按计划执行，且只推送新增内容。

产品口号：**让每条标讯可追溯，让每次推送不重样，让每个商机能行动。**

## 2. 目标用户与关键场景

- 市场与销售：发现与产品能力匹配的新商机。
- 采购与供应链：持续监控采购项目、供应商和行业变化。
- 合规与风控：保留来源证据，避免摘要失真与过期信息误判。
- 管理者：通过 Word 简报快速判断“本期新增、重点机会、来源覆盖与风险”。

核心用户流：

1. 输入“最近 3 个月上海充电桩招标信息，请每天 9:00 汇总后发送给我”。
2. 系统展示可编辑的结构化意图和置信度，确认抓取范围。
3. 多源并行采集公开站点和已授权的免费会员站点。
4. 清洗正文、提取附件、过滤不符记录、识别跨站重复和项目生命周期。
5. 生成带来源证据、覆盖说明和新增标识的 Word 报告。
6. 保存订阅并在后续执行中仅推送未送达的新版本。

## 3. 冠军级差异化

### 3.1 查询编译器，而非黑盒聊天

自然语言被编译成版本化 `TenderQuerySpec`，包含主题、同义词、地域、时间窗、公告类型、频率、时区与发送渠道。系统展示每个槽位的置信度和歧义提示，降低“听懂了但做错了”的风险。

### 3.2 证据账本：先证据、后摘要

每条摘要都绑定原始 URL、抓取时间、正文哈希和支持该结论的证据片段。LLM 仅在证据范围内重写；数字、日期、机构名无法回指原文时自动降级为抽取式摘要。报告显示来源健康度与覆盖缺口，不伪装“全网完整”。

### 3.3 项目生命周期图，而非粗暴删重

区分“同一公告的转载/镜像”和“同一项目的意向、招标、更正、中标、合同”等不同事件。前者聚合去重，后者串成项目时间线，既不重复轰炸，也不误删关键变更。

### 3.4 恰好一次增量推送

以“订阅 + 规范项目 ID + 公告版本哈希”为投递账本键。只有报告生成且发送成功后才登记送达；失败可重试但不会重复推送，更新公告则作为新版本再次提醒。

### 3.5 可插拔来源与登录态

首版接入中国招标投标网、中国政府采购网和千里马招标网（免费会员、用户主动授权登录态）。来源适配器具备速率限制、超时、健康检查、结构变化告警和独立降级，不绕过验证码或付费权限。

### 3.6 飞书闭环

提供飞书机器人推送与飞书开放平台文件发送；同时暴露 OpenAPI，可作为飞书 Aily 智能伙伴的工具，让用户在飞书会话中发起查询、创建订阅并接收报告。

## 4. 功能范围

### 4.1 必须实现

- Web UI、REST API、CLI 三种入口。
- 中文自然语言意图解析：主题、地域、时间范围、频率、发送时间。
- 至少 3 个来源适配器，其中至少 1 个需要用户授权登录。
- 列表与详情抓取、正文清洗、附件链接提取、条件过滤。
- 跨站去重、项目生命周期聚类、相关性与机会优先级评分。
- 证据约束摘要，离线抽取式降级。
- 符合命名规则的 `.docx` 报告。
- 立即执行、每日/每周定时执行、一次性未来执行。
- 增量投递账本，已推送内容不重复。
- 本地文件、飞书 Webhook、飞书开放平台三类投递通道。
- 来源状态、运行历史、报告下载、订阅管理与失败可诊断信息。

### 4.2 非目标

- 不代替用户投标、付款或购买会员。
- 不绕过验证码、访问控制、付费墙或网站条款。
- 不宣称覆盖所有互联网来源；报告必须披露实际来源与失败项。
- 不在未获授权时保存账号密码；登录态只保存在本机、不得提交仓库。

## 5. 质量与合规约束

- 报告中每条结果必须包含标题、发布时间、来源链接、核心内容、附件链接（如有）。
- 核心事实必须能回指原文；生成式摘要失败时必须安全降级。
- 代码不得硬编码竞赛示例结果；测试夹具只用于解析器回归测试。
- 对网站进行限速、超时、重试上限和来源级熔断。
- 所有引用研究材料在报名补充材料与设计文档中注明来源。
- 敏感配置放入 `.env` 或本地密钥目录，并通过 `.gitignore` 排除。

## 6. 交付物

- 完整源代码、测试、启动脚本和 Docker 配置。
- 多份真实运行生成的 Word 报告。
- `详设文档.docx/.md`、`操作手册.docx/.md`。
- 海选开题报告文字、补充材料 PDF/PPTX、参考资料清单。
- Demo 演示脚本、录屏分镜、路演讲稿与答辩问题库。
- 端到端测试记录、来源覆盖记录和评审自评分。

## 7. 竞赛依据

- 命题页：https://activity.feishu.cn/future-talent?detail=chaojubian
- 报名表：https://bytedance.larkoffice.com/share/base/form/shrcnY5Tj6HJhdtFhRkgNJvMiWe
- 详细赛制：https://bytedance.larkoffice.com/wiki/EwPtwUn5ciw0UlkF7dDcndyAn1n
- 中国政府采购网数据接口规范：https://www.ccgp.gov.cn/sjbzjgf/202403/t20240304_21594369.htm
- 中国招标投标网：https://www.cecbid.org.cn/search
- 中国政府采购网：https://www.ccgp.gov.cn/
- 千里马免费会员入口：https://wap.qianlima.com/register.jsp
- 剑鱼标讯产品能力：https://www.jianyu360.cn/product/index?serviceType=1
- 招标雷达产品能力：https://bidradar.com.cn/productFeatures.html

