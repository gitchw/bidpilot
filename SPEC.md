# 标擎 BidPilot：可验证招投标情报 Agent

## 1. 产品目标

面向超聚变“招投标信息聚合工具”命题，交付一个真实可运行、可验证、可持续订阅的 Web/API/CLI 系统。用户用中文描述主题、地域、时间和发送计划，系统从真实站点采集信息，严格筛选、跨站去重、关联项目生命周期、生成 Word 报告，并在后续执行中只提醒尚未成功确认投递的公告版本。

产品原则：**让每条标讯可追溯，让每次提醒有依据，让每个商机能行动。**

## 2. 目标用户与核心任务

- 市场与销售：持续发现与产品能力匹配的新商机。
- 采购与供应链：监控采购项目、采购单位和行业变化。
- 合规与风控：保留来源证据，避免摘要失真和覆盖夸大。
- 管理者：通过 Word 简报快速判断新增、优先级、覆盖和风险。

主流程：

1. 输入“最近 3 个月上海充电桩招标信息，请每天 9:00 汇总后发送给我”。
2. 查询编译器展示主题、地域、时间窗、计划、渠道、置信度和警告。
3. 用户立即执行，或创建一个可编辑的持久订阅。
4. 多源并行采集公开站点和已授权免费会员站点。
5. 清洗正文、提取附件、严格过滤、合并转载并保留生命周期事件。
6. 生成带来源证据和覆盖说明的 DOCX 报告。
7. 报告中心、飞书或邮件投递；成功确认后登记增量账本。
8. 用户在订阅中心修改规则、暂停、恢复、立即执行、查看日志或删除。
9. 用户把可信结果加入机会工作台，分配负责人和下一步，并沿项目生命周期持续跟进变更。
10. 用户在决策中心维护企业画像，查看逐条适配判断、风险、行动建议和本轮固定证据追问。
11. 用户用相关/无关/观察/已联系反馈校正后续排序，并从买方雷达把真实采购单位转成锁定监控。

## 3. 产品差异化

### 3.1 查询编译器，而非黑盒聊天

自然语言被编译成版本化 `TenderQuerySpec`：主题、同义词、地域、日期范围、计划、时区、渠道和槽位置信度均可检查。用户能在执行前看懂系统将做什么。

### 3.2 先证据、后摘要

每条记录保留原始 URL、正文证据片段、抓取时间、附件和来源集合。LLM 只能在证据范围内重写；缺少模型或事实校验失败时使用抽取式摘要。来源失败不会被无关内容填充。

### 3.3 转载去重与生命周期分离

同一公告的跨站转载被合并并保留全部来源；同一项目的采购意向、招标、更正、中标和合同是不同事件，不被粗暴删除。

### 3.4 成功确认后的版本级防重复

账本键为“订阅 + 规范公告 ID + 版本哈希”。只有报告生成且渠道返回成功后才登记；失败会重试，新版本会再次提醒。对外部渠道存在网络确认不确定窗口，因此不宣称分布式绝对恰好一次。

### 3.5 持久 worker，而非进程内临时闹钟

SQLite 持久化 `next_run_at`、运行、投递、worker 心跳和租约。worker 原子领取到期任务，执行期间续租；多进程不重复领取，崩溃后租约过期可接管，失败按上限退避。实现不依赖 Windows 计划任务。

### 3.6 来源与运维边界可见

来源中心显示每个适配器的官方/行业属性、公开/授权模式、配置状态、最近执行、抓取数、保留数和诊断。全国公共资源交易平台当前只覆盖首页最新公告流，千里马未授权时明确显示待授权。

### 3.7 证据库驱动的机会工作台

机会只能由 `tender_items` 中已有的真实记录创建，浏览器不能提交自造快照。同一 `project_key` 只保留一个机会；阶段、负责人、下一步、备注、标签和已读状态持久化。项目出现更正、中标或合同等新事件时，卡片自动更新到最新生命周期快照并重新标为未读，但不覆盖人工跟进信息。

### 3.8 可审计的多轮 AI 检索，而非无边界扩词

本地检索规划器先生成确定性主题、同义词、来源优先级和查询预算；可选模型只能提交严格 JSON 的受控补充词。系统按覆盖缺口最多执行两轮，日期、地域、公告类型和排除词始终由本地硬过滤；边界候选可由模型复核，但模型不能突破硬边界或伪造来源证据。网页逐轮展示查询词、扫描/候选/去重/保留数量、缺口触发原因和回退状态。

### 3.9 企业画像、适配判断与证据问答闭环

企业画像、反馈记忆和运行证据均持久化。模型只识别画像语义关联、选择固定 E 编号字段或逐字证据，标题、采购人、日期、地域、阶段、项目编号、URL、分数和最终建议由本地校验或回填。模型未配置、超时、JSON 非法或引用未知证据时使用确定性回退，不中断主任务。买方雷达只聚合本地真实公告，不生成联系人或采购预测。

## 4. 当前功能范围

### 4.1 已实现

- Web UI、REST API、CLI。
- 中文自然语言查询与每日、每周、未来一次性计划。
- 10 个真实来源适配器：
  - 中国招标投标网；
  - 中国政府采购网；
  - 全国公共资源交易平台；
  - 商务部中国国际招标网；
  - 千里马招标网（用户授权免费会员会话）。
- 中国招标投标公共服务平台；
- 中央政府采购网；
- 军队采购网；
- 广东省政府采购网；
- 深圳公共资源交易中心。
- 来源级超时、限速、有限重试、独立失败和覆盖披露。
- 规则基线 + 可选严格 JSON 模型建议的混合意图引擎。
- 可审计检索计划、最多两轮补搜、来源查询预算、覆盖缺口和语义边界复核。
- 正文清洗、附件提取、地域/时间/主题严格过滤。
- 转载去重、项目生命周期聚类、相关性和机会评分。
- 离线摘要与可选 OpenAI-compatible 证据约束摘要。
- 可下载 DOCX 报告。
- SQLite 持久订阅、失败退避、租约互斥、续租和重启恢复。
- 本地、飞书 Webhook、飞书应用、SMTP 邮件、钉钉、企业微信与通用 Webhook 投递。
- 网页运行时配置中心：模型和投递渠道即时生效、重启持久化、敏感值加密且只返回配置状态。
- 可编辑订阅规则、通知策略、暂停/恢复、立即执行、日志和删除。
- 来源中心、报告历史、系统/worker 在线状态。
- 来源健康历史、时延、产出趋势和网页可见授权会话管理。
- 项目级机会工作台、阶段看板、搜索筛选、负责人/待办/标签/备注/已读管理。
- 单项目生命周期时间线，以及新事件自动刷新与未读提醒。
- 企业画像、证据约束的适配判断、AI 情报简报、反馈学习和本轮固定证据追问。
- 买方雷达及从真实采购单位创建的 `buyer_keywords` 锁定订阅。
- 50 个带完整中文用途、参数、返回、副作用、错误和示例的 OpenAPI 操作。
- Python 跨平台引导、Dockerfile、Compose 独立 worker、三系统 CI 矩阵。

### 4.2 下一阶段产品能力

在当前工程稳定后，再按价值顺序加入：

1. 竞争对手与合作伙伴的证据化监控，不猜测联系人或非公开关系。
2. 机会待办日历、批量分配、权限模型和可导出协作视图。
3. 可配置但仍受证据约束的评分权重、审批流和投标阶段模板。
4. 来源结构变化告警、自动回归沙箱和人工确认后的适配器发布流程。
5. 在完成身份认证、角色权限、审计和 TLS 后提供团队部署模式。

这些方向参考了剑鱼标讯的组合订阅、收藏、项目/企业监控和线索分配，以及招标雷达、雷达云的语义匹配、匹配度和项目跟踪；只实现能被真实数据和测试支撑的部分。

### 4.3 明确非目标

- 不代替用户投标、付款、购买会员或联系采购方。
- 不绕过验证码、付费墙、访问控制或网站条款。
- 不宣称覆盖整个互联网或所有历史公告。
- 不在仓库保存密码、Cookie、API 密钥或个人身份信息。
- 不用硬编码比赛示例或伪造成功率、节省时间等效果数据。

## 5. 工程架构

### 5.1 入口与编排

- `bidpilot/api.py`：FastAPI、静态 Web、REST 管理端点。
- `bidpilot/cli.py`：解析、运行、服务、worker、来源检查、授权、OpenAPI 导出。
- `bidpilot/service.py`：运行、订阅、报告、投递和状态编排。
- `bidpilot/control.py`：跨平台服务发现、本机令牌、状态、停止和重启控制。

### 5.2 数据流水线

- `intent.py` / `hybrid_intent.py`：规则基线、字段置信度、严格 JSON 模型提议与逐字段合并。
- `retrieval.py`：检索计划、多轮补搜、覆盖缺口和语义边界复核。
- `sources/`：独立来源适配器。
- `fetch.py`：异步 HTTP、按域限速、有限重试。
- `normalize.py` / `summarize.py`：筛选、规范化、去重、生命周期和摘要。
- `intelligence.py` / `decision.py` / `evidence_qa.py`：情报简报、企业适配和固定证据追问。
- `buyer_radar.py`：只基于本地公告的采购单位聚合与锁定监控入口。
- `report.py`：DOCX 报告。

### 5.3 持久执行与交付

- `db.py`：SQLite 事务、迁移、运行、报告、投递账本、worker 和租约。
- `scheduler.py`：下一次时间计算、退避、worker 心跳、领取和续租。
- `delivery.py`：本地、飞书、SMTP、钉钉、企业微信和通用 Webhook。
- `runtime_config.py` / `source_auth.py`：网页配置加密持久化与用户可见授权会话。

## 6. 关键不变量

1. 没有来源证据的事实不得进入可信摘要。
2. 任一来源失败不得拖垮其他来源；报告必须披露失败或部分覆盖。
3. 同一订阅的自动执行与手动执行不得并发。
4. 投递失败不得写入增量账本。
5. 只有租约所有者能续租；过期租约可以被其他 worker 接管。
6. 暂停订阅后不再被 worker 领取；恢复时重新计算未来时间。
7. 修改自然语言规则后重新计算计划，但保留既有账本以避免重复。
8. 未配置的外部渠道不得被呈现为可用推送。
9. 所有下载路径必须限制在报告目录内。
10. 敏感配置不得进入 Git、日志、报告或 API 状态响应。
11. `/docs` 每个业务操作必须包含中文用途、参数、返回、副作用、错误码和示例。
12. 普通“招标信息/采购信息”必须保留项目生命周期；只有明确公告类型表达才做类型过滤。
13. 未内置多用户鉴权前，服务默认只监听本机；公网部署必须置于带 TLS 和访问控制的反向代理之后。
14. 机会只能引用证据库中的真实记录，同一项目不得产生重复机会。
15. 新生命周期事件可以刷新机会快照和未读状态，但不得覆盖负责人、阶段、下一步、备注或标签。
16. 模型不能提交来源 URL、身份事实、适配分或最终建议；所有模型选择必须通过本地证据与枚举校验。
17. 业务数据目录与生命周期控制目录可以分离，普通 `status/stop/restart` 仍必须发现同一项目的可管理服务。

## 7. 验收门槛

- 四条命题示例和边界表达解析通过。
- 10 个来源均有诚实能力元数据和失败隔离；固定评测保留可核验原文 URL。
- 首次订阅产生 N 条，第二次来源不变时新增为 0，账本数量不增长。
- 投递故障后账本为 0、投递尝试失败、下次重试时间已安排。
- 两 worker 不能同时领取；长任务续租；租约过期后可恢复。
- 真实进程重启后订阅 ID、下次时间、运行历史和投递历史不丢失。
- Web 完成查询、报告、订阅创建与完整管理闭环。
- Web 可从真实结果创建项目级机会，完成跟进编辑、搜索、阶段筛选、已读切换和证据时间线；重启后状态不丢失。
- Web 可完整管理企业画像、适配判断、反馈、证据追问、买方雷达和来源授权，不要求编辑配置文件。
- 默认端口为 8000；真实子进程 `serve → status → restart → stop` 通过，数据库、报告、控制目录与版本均可见且停止后状态文件清理。
- OpenAPI 操作数为 50，独立 API 参考与 schema 同步。
- Ruff、完整 Pytest、JavaScript 语法和 `git diff --check` 全部通过。
- Docker 只能在实际构建成功后标记为已验证。

## 8. 参考依据

- 命题页：https://activity.feishu.cn/future-talent?detail=chaojubian
- 报名表：https://bytedance.larkoffice.com/share/base/form/shrcnY5Tj6HJhdtFhRkgNJvMiWe
- 中国政府采购网：https://www.ccgp.gov.cn/
- 全国公共资源交易平台：https://www.ggzy.gov.cn/
- 商务部中国国际招标网：https://chinabidding.mofcom.gov.cn/
- 中国招标投标网：https://www.cecbid.org.cn/search
- 千里马会员服务：https://vip.qianlima.com/tradeCenter_Service.html
- 剑鱼标讯订阅：https://www.jianyu360.cn/front/subscribe.html
- 招标雷达产品功能：https://bidradar.com.cn/productFeatures.html
- 雷达云产品文档：https://www.leidayun.com/home/docs.html
